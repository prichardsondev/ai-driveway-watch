# Prompt: calibrate camera zones

Use this repository’s `AGENTS.md` privacy and deployment rules.

Connect to my Raspberry Pi using the host and account I provide privately.
Inspect one current dashboard frame without printing the camera URL or `.env`.
Help me calibrate the normalized driveway, mailbox, and road polygons from my
plain-language landmarks.

Requirements:

1. show me a proposed overlay before saving;
2. keep driveway arrivals, mailbox stops, and passing traffic independent;
3. back up the private configuration before changing it;
4. compile first if code changes are required;
5. restart only the driveway service;
6. verify camera health and all three overlays;
7. do not generate a fake notification or delete an event.
